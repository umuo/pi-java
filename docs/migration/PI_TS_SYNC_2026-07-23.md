# TS 上游同步记录（2026-07-23）

## 对比基线

- TS 仓库：`/Volumes/Data 1/github/pi`
- 拉取前提交：`54113731b2e70ceb61ea1948fdfe83395ff2fd90`
- 本次同步提交：`9b3a2059171bcc74ad9d2cadeea6d186776cf2db`
- 对比范围：`54113731b2e70ceb61ea1948fdfe83395ff2fd90..9b3a2059171bcc74ad9d2cadeea6d186776cf2db`
- 上游规模：283 个提交、546 个文件，新增 33,768 行、删除 25,545 行
- 对齐版本：`0.81.1`（Java 开发版本为 `0.81.1-SNAPSHOT`）

供下次同步读取的唯一基线保存在 [`PI_TS_UPSTREAM_COMMIT`](PI_TS_UPSTREAM_COMMIT)。

## 已移植到 Java

- AI 模型语义：`max` thinking level、tool choice、内容归一化、usage 统计修正。
- Provider：Qwen Token Plan 国际/中国、Moonshot 国际/中国、xAI Grok 4.5 Responses API、OpenAI-compatible tool output 和 cache usage 兼容。
- 稳定性：HTTP 524、DNS/连接中断/early EOF 重试，assistant、compact、branch summary 的重试与 usage 传递。
- Agent/session：`agent_settled`、tool result usage/added tools、compaction/branch usage、短 UUIDv7 entry ID、session metadata/name/stats/path 查询。
- 存储：新增 JDBC SQLite session storage Maven 模块和迁移/查询/事务测试。
- Coding agent：bash 环境变量隔离与 session/model 信息注入、`~` 展开、timeout 校验、RPC 可用 thinking levels、系统提示缓存稳定性。
- 结构与版本：`orchestrator` 模块改名为 `server`，Maven 项目版本更新为 `0.81.1-SNAPSHOT`。

## 未直接移植

以下变更依赖 TS/Node 特有实现，或 Java 当前尚无对应用户界面/运行时，因此没有机械照搬：

- Node 原生扩展加载、动态 provider、OAuth/OpenRouter/Kimi Code 登录流程。
- llama.cpp router、Hugging Face 下载器和模型目录代码生成流水线。
- OpenAI WebSocket cache affinity 与 Node SDK 的 transport 细节。
- TS 全屏 TUI 的软件光标、paste registry、ANSI wrapping 和外部编辑器启动优化。
- npm 发布、release source archive、GitHub Actions 与 lockfile 维护。

这些仍属于 [`PI_TS_EXCELLENT_FEATURES_NOT_MIGRATED.md`](PI_TS_EXCELLENT_FEATURES_NOT_MIGRATED.md) 中记录的 Java 功能缺口；后续同步应从上述最新提交继续比较，不重复迁移本记录已覆盖的语义。
